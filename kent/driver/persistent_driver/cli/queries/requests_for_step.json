{
    "schema_version": 20,
    "description": "List requests (id, status, url) for a given continuation (step name).",
    "query": "SELECT id, status, url FROM requests WHERE continuation = :step ORDER BY id;",
    "params": ["step"]
}
